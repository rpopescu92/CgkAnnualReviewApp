spring:
   datasource:
        url: jdbc:postgresql://localhost:5432/Cegeka
        username: postgres
        password: root
        driverClassName: org.postgresql.Driver
   jpa:
     database-platform: org.hibernate.dialect.PostgreSQL9Dialect
     hibernate:
          show_sql: true
          ddl-auto: update